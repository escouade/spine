// @spinejs/throttle/testing — the store contract kit (FR-20): reusable assertions pinning the AD-4
// store semantics (atomicity, relative resetMs, accepted-hits-only growth, unconditional
// consumption, reclamation), so any store implementation validates without copying tests.
// Content lands with Story 1.6.
export {};
