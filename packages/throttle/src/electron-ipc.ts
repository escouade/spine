// @spinejs/throttle/electron-ipc — the electron IPC preset (AD-1): the `'sender'` key source
// (`event.sender.id` — never `'ip'`). Optional peer: `@spinejs/electron-ipc-gateway`.
// Content lands with Story 1.10.
export {};
